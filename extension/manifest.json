{
  "manifest_version": 2,
  "name": "FetchGate",
  "version": "0.1.0",
  "description": "Proxy authenticated fetch() calls from an active browser tab to an external process.",

  "permissions": [
    "nativeMessaging",
    "activeTab",
    "tabs",
    "<all_urls>"
  ],

  "background": {
    "scripts": ["background.js"],
    "persistent": true
  },

  "browser_action": {
    "default_title": "FetchGate — click to arm/disarm this tab",
    "default_icon": "icon.svg"
  },

  "browser_specific_settings": {
    "gecko": {
      "id": "fetchgate@localhost"
    }
  }
}
